/*
import 'package:flutter/cupertino.dart';

import '8 cart model.dart';

final List<Map<String, dynamic>> data = [];

class CartProvider with ChangeNotifier {
  final List<Cart> plantHome = data.cast<Cart>();

  List<Cart> get movies => plantHome;
  final List<Cart> wishlist = [];

  List<Cart> get wishplant => wishlist;

  void addToList(Cart plant) {
    wishlist.add(plant);
    notifyListeners();
  }

  void removeFromList(Cart plant) {
    wishlist.remove(plant);
    notifyListeners();
  }
}
*/
